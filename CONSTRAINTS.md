# CONSTRAINTS.md
本ドキュメントは SRS.md（仕様）を補完し、コード自動生成エンジン（例: Codex）に対して**守るべき厳格な制約**を与えるものです。生成用プロンプトから参照されることを前提に、曖昧さを排し、再現性と保守性を高めます。

## 1. 対象リポジトリと責務境界
- 仕様の出所: SRS.md（唯一の真実源）。本ファイルはその拘束条件。
- 技術スタック: 
 - Python 3.11 系（core: 前処理、学習・推論、スコアリング、閾値設計、説明可能性）
 - PyTorch 2.x（LSTM 実装、学習）
 - MATLAB/Simulink R2023b+（ブロック化・比較・可視化）
- OS: Windows 11 / Ubuntu 22.04 の双方で動作（GPU は任意）
- データ I/O: ローカルファイルのみ。ネットワークアクセス禁止（許可が明記される場合を除く）。

## 2. ディレクトリ規約（生成時の前提）
- src/ パッケージ配下に機能を分割
 - src/dataio: ログ読み込み、スキーマ検証、Δt (delta t)計算
 - src/features: 文字列→イベント符号化、時間特徴、セッション化
 - src/models: LSTM（制御器解釈を意識した API）、しきい値モジュール、説明可能性
 - src/eval: 指標計算、再現レポート
 - src/export: Simulink 連携（ONNX/Mat ファイル出力、ポート定義のメタ）
- scripts/: CLI エントリ（ingest, train, score, threshold, explain, export-simulink）
- configs/: YAML 設定（実験、モデル、しきい値、可視化）
- simulink/: .slx, .mdl, 付随の .m スクリプト
- tests/: pytest による単体・統合テスト
- docs/: ドキュメント（README, SRS, 本ファイル, ほか）

## 3. データ制約（列挙）
- 入力ログの必須列: timestamp, session_id, user_id, event, message, level, module
- オプション列: params（JSON 互換の辞書）, latency_ms, status_code, host
- タイムゾーン: すべて UTC に正規化
- Δt (delta t) の定義: セッション内で同一 user_id の連続イベント間の経過秒（float）
- 欠損時刻: 前件欠落・逆順は除外またはセッション断絶として扱う（設定で選択）
- セッション化: idle_timeout 秒で区切り（デフォルト 1800）

## 4. モデル/学習制約
- LSTM 入力: [イベント埋め込み, Δt (delta t)（標準化）, 重大度、モジュールID、数値パラメータ統計] の連結
- 損失: 予測型（次イベント確率/パターン逸脱）または再構成型を SRS に従い選択
- 乱数固定: Python, NumPy, PyTorch で seed を統一
- 訓練/検証/テスト分割: セッション単位の時系列分割。ランダムシャッフル禁止
- 早期終了・学習率スケジューラの使用を許可（設定で切替）

## 5. 閾値設計/異常スコア制約
- スコアの候補: 予測確率の負対数、復元誤差、確率分位、KS 最適化、BOP（ベイズ最適点）
- 閾値選択: 検証集合での F1 最大、もしくは監視要件に基づく TPR/TNR ターゲットの交点
- 多段閾値: 重大度（level）に応じた階層しきい値をサポート

## 6. 説明可能性（XAI）制約
- 単体イベント寄与度: SHAP/Integrated Gradients いずれか（実装選択は設定）
- 重要: 再現可能な種と固定バッチで同一説明が得られること
- 出力: JSON（feature_importances）と CSV（人可読）両方を保存

## 7. Simulink 連携制約
- エクスポート形式: ONNX または重み .pt + 入出力仕様 .json
- ブロック I/F: 入力（イベントID, Δt (delta t), 他特徴の連結ベクトル）, 出力（制御アクション or 正常確率）
- ユーザ別ブロック: user_id ごとにハイパラ差分を吸収。切替ロジックは外部（Switch/Variant Subsystem）
- PID 比較: 同一テストシナリオで LSTM と PID の出力を比較。可視化は数表（画像出力は任意）

## 8. コーディング標準
- PEP8 + 型注釈（mypy 通過）
- 例外は ValueError/RuntimeError/IOError を中心に明確化
- ログ出力は JSON 構造で stdout（INFO 以上）
- 関数長は 50 行未満、モジュールは 400 行目安

## 9. テスト/CI
- pytest によるカバレッジ 80% 以上
- データの最小再現サンプル（小さな fixtures）を同梱
- 各 CLI は `--help` が通ること、終了コード 0

## 10. ライセンス/セキュリティ
- 外部データ送信禁止、個人情報（user_id など）は疑似化
- 依存は明示（requirements.txt, environment.yml）
- OSS ライセンス遵守（Simulink 関連は社内/学内配布範囲で扱う）

## 11. 生成出力の形式（**厳守**）
- 原則として**パッチ形式（unified diff）**または**新規ファイルの完全内容**のみを出力。余談や説明文は先頭に最小限の要約 3 行以内。
- 既存ファイルを編集する場合、差分にはファイルパス、@@ hunk を含めること。
- バイナリは生成しない（コード/テキストのみ）。

## 12. あいまいさへの対応
- SRS/本ファイルに記述が無い事項は**安全側**に倒す（保守・再現性優先）。
- 致命的に不明な場合は、必要最小限の質問を 1 回だけ付すか、`TODO:` としてコード内に明示して先に進める。

- 参照ファイル: README.md
- 参照ファイル: dev_prompt.md
