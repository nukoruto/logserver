{
  "name": "@logserver/session-splitter",
  "version": "0.1.0",
  "description": "Session splitting utilities shared across CLI and Electron UI",
  "type": "module",
  "main": "dist/index.cjs",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run typecheck && tsup src/index.ts --dts --format esm,cjs --sourcemap --clean",
    "typecheck": "tsc --noEmit",
    "test": "pnpm run build && vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "csv-parse": "^5.5.6"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "tsup": "^8.0.1",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
